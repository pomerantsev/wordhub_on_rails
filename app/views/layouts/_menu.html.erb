<div class="navbar">
    <div class="navbar-inner">
        <% if logged_in %>
            <a class="btn btn-navbar fat-menu-center no-js-hide" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            
            <ul id = "elementBeforeDropdown" class = "nav pull-left">
                <li class = "">
                    <%= link_to new_flashcard_path, :class => "" do %>
                        <span>Создать</span>
                         <!-- Пока до конца не понял, выводит ли created_on правильное значение, потому что Date.today в общем случае возвращает одно значение, а в базе сохраняется другое (связано с часовыми поясами) -->
                        <span><small><%= current_user.flashcards.created_on(session[:date]).size %> из <%= current_user.daily_limit %></small></span>
                    <% end %>
                </li>
                <li class = "">
                    <%= link_to repetitions_path, :class => "" do %>
                        <span>Повторить</span>
                        <span><small><%= current_user.repetitions.run.on(session[:date]).size %> из <%= current_user.repetitions.for(session[:date]).size %></small></span>
                    <% end %>
                </li>
                
            </ul>
            
            <!-- Этот ul Яваскриптом обрамляется в div, чтобы в случае, если Яваскрипт не работает, меню работало, хоть и выглядело бы некрасиво. -->
                <ul id = "dropdownMenu" class="fat-menu-center nav pull-right"> <!-- Класс fat-menu-center - чтобы пункты меню в "толстом" меню не были приклеены к верху, а появлялись посередине. -->
                    <li class = "">
                        <%= link_to "Все карточки", flashcards_path %>
                    </li>
                    <li class = "">
                        <%= link_to "О сайте", :controller => "static_pages", :action => "intro" %>
                    </li>
                    
                    <!-- Дропдаун подсвечивается как активный, когда выбран один из пунктов меню, входящих в него. -->
                    <li id = "userDropdownMenu" class="no-js-hide dropdown">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.name %> <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li class = "<?php echo getMenuItemClass($activePage, SETTINGS_PAGE); ?>">
                                <a href="<?php echo getLink(SETTINGS_PAGE); ?>">Настройки</a>
                            </li>
                            <li class="divider"></li>
                            <li>
                                <%= link_to "Выйти", "/logout" %>
                            </li>
                        </ul>
                    </li>
                    <li id = "userMenuNoDropdown">
                        <ul class = "nav">
                            <li class = "">
                                <a href="#">Настройки</a>
                            </li>
                            <li class = "divider"></li>
                            <li>
                                <%= link_to "Выйти", "/logout" %>
                            </li>
                        </ul>
                    </li>
                </ul>
        <% else %>
            <%= link_to "Вордхаб", {:controller => "application"}, :class => "brand" %>
            <ul class="nav pull-right">
                <li>
                    <%= link_to "О сайте", :controller => "static_pages", :action => "intro" %>
                </li>
                <li>
                    <%= link_to "Вход", :controller => "application", :action => "index", :anchor => "login" %>
                </li>
            </ul>
        <% end %>
    </div>
</div>




