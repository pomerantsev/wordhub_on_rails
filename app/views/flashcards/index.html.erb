<% @flashcards.each do |flashcard| %>
    <% added_class = flashcard.learned? ? " flashcard-learned" : "" %>
    <%= link_to html_escape(flashcard.front_text), flashcard_path(flashcard), :class => "hero-unit flashcard" + added_class %>
<% end %>
